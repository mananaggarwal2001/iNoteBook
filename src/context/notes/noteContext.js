// syntax for creating the context for the notes api in the given note which is :-
import { createContext } from "react";

const noteContext= createContext(); // this is the syntax for creating the context.

export default noteContext // this is use for exporting the notecontext in the react application.